<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Edit Catatan</title>
  <link rel="stylesheet" href="/styles.css">
</head>
<body>
  <div class="container">
    <h1>Edit Catatan Keuangan</h1>
    <form action="/catatan/<%= catatan.id %>?_method=PUT" method="POST">
      <div class="form-group">
        <label for="id">ID</label>
        <input type="number" id="id" name="id" value="<%= catatan.id %>" readonly>
      </div>
      <div class="form-group">
        <label for="type">Tipe</label>
        <select id="type" name="type" required>
          <option value="INCOME" <%= catatan.type === 'INCOME' ? 'selected' : '' %>>INCOME</option>
          <option value="EXPENSE" <%= catatan.type === 'EXPENSE' ? 'selected' : '' %>>EXPENSE</option>
        </select>
      </div>
      <div class="form-group">
        <label for="amount">Jumlah</label>
        <input type="number" id="amount" name="amount" value="<%= catatan.amount %>" required>
      </div>
      <div class="form-group">
        <label for="description">Deskripsi</label>
        <input type="text" id="description" name="description" value="<%= catatan.description %>" required>
      </div>
      <button type="submit" class="btn btn-add">Simpan</button>
      <a href="/catatan" class="btn btn-cancel">Batal</a>
    </form>
  </div>
</body>
</html>